/**
 * Experience + education block in `#resume`.
 * - `timeline`: centered spine, cards alternate left / right.
 * - `rail`: single column with left rail; full-width glass cards.
 * - `briefcase`: two-column meta | description, amber accents (CV-style reference).
 */
export type ExperienceSectionLayout = 'timeline' | 'rail' | 'briefcase';

export const experienceSectionLayout: ExperienceSectionLayout = 'briefcase';
